CREATE OR REPLACE EXTERNAL TABLE `source.crashes` (
    `ARRIVAL_TM` STRING,
    `AUTOMOBILE_COUNT` STRING,
    `BELTED_DEATH_COUNT` STRING,
    `BELTED_SUSP_SERIOUS_INJ_COUNT` STRING,
    `BICYCLE_COUNT` STRING,
    `BICYCLE_DEATH_COUNT` STRING,
    `BICYCLE_SUSP_SERIOUS_INJ_COUNT` STRING,
    `BUS_COUNT` STRING,
    `CHLDPAS_DEATH_COUNT` STRING,
    `CHLDPAS_SUSP_SERIOUS_INJ_COUNT` STRING,
    `COLLISION_TYPE` STRING,
    `COMM_VEH_COUNT` STRING,
    `CONS_ZONE_SPD_LIM` STRING,
    `COUNTY` STRING,
    `CRASH_MONTH` STRING,
    `CRASH_YEAR` STRING,
    `CRN` STRING,
    `DAY_OF_WEEK` STRING,
    `DEC_LAT` STRING,
    `DEC_LONG` STRING,
    `DISPATCH_TM` STRING,
    `DISTRICT` STRING,
    `DRIVER_COUNT_16YR` STRING,
    `DRIVER_COUNT_17YR` STRING,
    `DRIVER_COUNT_18YR` STRING,
    `DRIVER_COUNT_19YR` STRING,
    `DRIVER_COUNT_20YR` STRING,
    `DRIVER_COUNT_50_64YR` STRING,
    `DRIVER_COUNT_65_74YR` STRING,
    `DRIVER_COUNT_75PLUS` STRING,
    `EST_HRS_CLOSED` STRING,
    `FATAL_COUNT` STRING,
    `HEAVY_TRUCK_COUNT` STRING,
    `HORSE_BUGGY_COUNT` STRING,
    `HOUR_OF_DAY` STRING,
    `ILLUMINATION` STRING,
    `INJURY_COUNT` STRING,
    `INTERSECT_TYPE` STRING,
    `INTERSECTION_RELATED` STRING,
    `LANE_CLOSED` STRING,
    `LATITUDE` STRING,
    `LN_CLOSE_DIR` STRING,
    `LOCATION_TYPE` STRING,
    `LONGITUDE` STRING,
    `MAX_SEVERITY_LEVEL` STRING,
    `MCYCLE_DEATH_COUNT` STRING,
    `MCYCLE_ SUSP_SERIOUS_INJ_COUNT` STRING,
    `MOTORCYCLE_COUNT` STRING,
    `MUNICIPALITY` STRING,
    `NONMOTR_COUNT` STRING,
    `NONMOTR_DEATH_COUNT` STRING,
    `NONMOTR_SUSP_SERIOUS_INJ COUNT` STRING,
    `NTFY_HIWY_MAINT` STRING,
    `PED_COUNT` STRING,
    `PED_DEATH_COUNT` STRING,
    `PED_ SUSP_SERIOUS_INJ_COUNT` STRING,
    `PERSON_COUNT` STRING,
    `POLICE_AGCY` STRING,
    `POSSIBLE_INJ_COUNT` STRING,
    `RDWY_SURF_TYPE_CD` STRING,
    `RELATION_TO_ROAD` STRING,
    `ROAD_CONDITION` STRING,
    `ROADWAY_CLEARED` STRING,
    `SCH_BUS_IND` STRING,
    `SCH_ZONE_IND` STRING,
    `SECONDARY_CRASH` STRING,
    `SMALL_TRUCK_COUNT` STRING,
    `SPEC_JURIS_CD` STRING,
    `SUSP_MINOR_INJ_COUNT` STRING,
    `SUSP_SERIOUS_INJ_COUNT` STRING,
    `SUV_COUNT` STRING,
    `TCD_FUNC_CD` STRING,
    `TCD_TYPE` STRING,
    `TFC_DETOUR_IND` STRING,
    `TIME_OF_DAY` STRING,
    `TOT_INJ_COUNT` STRING,
    `TOTAL_UNITS` STRING,
    `UNB_DEATH_COUNT` STRING,
    `UNB_ SUSP_SERIOUS_INJ_COUNT` STRING,
    `UNBELTED_OCC_COUNT` STRING,
    `UNK_INJ_DEG_COUNT` STRING,
    `UNK_INJ_PER_COUNT` STRING,
    `URBAN_RURAL` STRING,
    `VAN_COUNT` STRING,
    `VEHICLE_COUNT` STRING,
    `WEATHER1` STRING,
    `WEATHER2` STRING,
    `WORK_ZONE_IND` STRING,
    `WORK_ZONE_LOC` STRING,
    `WORK_ZONE_TYPE` STRING,
    `WORKERS_PRES` STRING,
    `WZ_CLOSE_DETOUR` STRING,
    `WZ_FLAGGER` STRING,
    `WZ_LAW_OFFCR_IND` STRING,
    `WZ_LN_CLOSURE` STRING,
    `WZ_MOVING` STRING,
    `WZ_OTHER` STRING,
    `WZ_SHLDER_MDN` STRING,
    `WZ_WORKERS_INJ_KILLED` STRING
)
OPTIONS (
    uris = ['gs://509final_raw_data/crashes/crashes.csv'],
    format = 'csv'
)
